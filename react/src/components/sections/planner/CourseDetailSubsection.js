import React, { Component } from 'react';
import { appBoundClassNames as classNames } from '../../../common/boundClassNames';

class CourseDetailSubsection extends Component {
    render() {
        return(
            <div className={classNames('subsection', 'subsection--course-info-detail')}>
                CourseDetailSubsection
            </div>
        );
    }
}

export default (
    CourseDetailSubsection
  );