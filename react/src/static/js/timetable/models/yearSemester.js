/* global Backbone */
var app = app || {};

(function () {
  'use strict';

  // YearSemester Model
  // ---------------
  
  var yearSemester = Backbone.Model.extend({
  });

  app.YearSemester = new yearSemester();
})();
