/* global Backbone */
var app = app || {};

(function () {
  'use strict';

  // Lecture Model
  // -------------
  
  app.Lecture = Backbone.Model.extend({
    sync : function(){},
    save : function(){},
  });
})();
